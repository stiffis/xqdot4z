\begin{figure}[t]
\centering
\begin{tikzpicture}[
 font=\scriptsize,
 box/.style={draw,minimum height=5.4mm,inner xsep=3pt,align=center},
 lbl/.style={font=\scriptsize\bfseries},
 arr/.style={-{Stealth[length=1.4mm]},shorten >=1pt}]

\node[box,align=center] (in) at (0,0.8)
 {\ifSpanish 8 pesos U4 y 4 activaciones S8, empacados
  \else 8 U4 weights and 4 S8 activations, packed\fi};

\foreach \y/\code in {-0.8/B1, -1.65/B2, -2.5/B3, -3.35/D}{
  \node[lbl] at (-3.62,\y) {\code};
}

\node[box,text width=21mm] (b1a) at (-1.80,-0.8)
 {\ifSpanish desplazar y sumar\else shift and add\fi};
\node[box,text width=21mm] (b1b) at (0.95,-0.8)
 {\ifSpanish restar 4 veces\else subtract 4 times\fi};

\node[box,text width=21mm] (b2a) at (-1.80,-1.65) {4 $\times$ MUL};
\node[box,text width=21mm] (b2b) at (0.95,-1.65)
 {\ifSpanish restar 4 veces\else subtract 4 times\fi};

\node[box,text width=21mm] (b3a) at (-1.80,-2.5) {XQDot4 $\rightarrow P$};
\node[box,text width=21mm] (b3b) at (0.95,-2.5) {$P - z\,S_a$};

\node[box,text width=21mm,very thick] (da) at (-1.80,-3.35) {XQDot4Zi};
\node[text width=21mm,align=center] (db) at (0.95,-3.35)
 {\ifSpanish (ya corregido)\else (already corrected)\fi};

\node[box,text width=11mm] (out) at (3.02,-2.07) {$d$ (INT32)};

\foreach \a/\b in {b1a/b1b, b2a/b2b, b3a/b3b, da/db}{\draw[arr] (\a) -- (\b);}
\draw (in.south) -- (0,0.42) -- (-3.18,0.42);
\foreach \n in {b1a,b2a,b3a,da}{\draw[arr] (-3.18,0.42) |- (\n.west);}
\foreach \n in {b1b,b2b,b3b,db}{\draw (\n.east) -| (2.26,-2.07);}
\draw[arr] (2.26,-2.07) -- (out.west);
\end{tikzpicture}
\caption{\ifSpanish Trabajo que hace cada variante sobre un mismo grupo de cuatro
pesos. B1--B3 son puntos de referencia; D es el diseño en estudio. Las cuatro
leen los mismos datos empacados, así que la diferencia no está en el producto
sino en quién paga la corrección del punto cero: B1 y B2 la restan término a
término, B3 la factoriza y reutiliza $S_a$ entre las $N$ filas, y D la absorbe
dentro de la propia instrucción.
\else Work each variant performs on one group of four weights. B1--B3 are points
of reference; D is the design under study. All four read the same packed data,
so the difference is not in the product but in who pays for the zero-point
correction: B1 and B2 subtract it term by term, B3 factors it and reuses $S_a$
across the $N$ rows, and D absorbs it inside the instruction itself.\fi}
\label{fig:comparison}
\end{figure}
