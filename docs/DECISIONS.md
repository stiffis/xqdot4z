# Decisiones de diseño y gestión

| ID | Decisión / estado | Motivo y condición de revisión |
|---|---|---|
| D01 | Implementada inicialmente: Kuntur, clon Git independiente en `kuntur/`, con correcciones comunes | Historial de `research/core-corrections` preservado; organización Git actual en D25. Original y snapshot intactos; no se prevé otro core activo en `rtl/core/` |
| D02 | Adoptada: snapshot histórico separado de baselines causales | Las correcciones no se atribuyen a XQDot4Z |
| D03 | Adoptada: U4/S8, za=0, subtotal exacto INT32 | Contrato inicial acotado, independiente de calidad ML |
| D04 | Adoptada: unidad aislada antes de integrar | Separar fallos aritméticos de control; sin FPGA |
| D05 | Fijada en contrato numérico v0.1: selección lo/hi de W | Ocho U4 en W y cuatro S8 en A; h solo selecciona pesos. Fuentes y encoding inmediato se concretan en D06/D07; ZR sigue abierto |
| D06 | M3a: inmediato z de cuatro bits; ZR sigue abierto | Dos GPR fuente, sin estado oculto. No representa gratuitamente z leído en ejecución; alternativas en `isa/SPEC.md` |
| D07 | Prototipo ISA v0.1: custom-0, instrucción de 32 bits | `xqdot4zi`, z[31:28], h[27], reservados [26:25]=00 y funct3=000; encoding comprobado contra GNU |
| D08 | Adoptada: comparación B3 con Sa reutilizable | Aislar el valor real de fusionar corrección |
| D09 | Implementada: política ISA explícita en RTL; pruebas con ensamblador | Se rechazan instrucciones no implementadas; no se afirma RV32I completo |
| D10 | Cerrada 2026-09-09: memoria parametrizable; capacidad común de medida fijada tras generar kernels | La propuesta de 64 KiB + 64 KiB NO se adoptó. El banco de medida usa 32 768 palabras de instrucciones y 1024 de datos, comunes a las cuatro variantes y dimensionadas por el brazo mayor, no por la conveniencia de ninguna. Ver D36 |
| D11 | Abierta: FPGA, herramienta de síntesis y frecuencia | Depende de acceso universitario; no bloquea M1/M2 |
| D12 | Sustituida en v0.2: IEEEtran conference, dos columnas, ES/EN, sin tema visual | Por indicación del autor: blanco y negro, también en TikZ; sin Nord ni otra paleta; convocatoria todavía abierta |
| D13 | Abierta: autores, contribuciones, afiliación y licencia | Confirmar con participantes; no inventar ni asignar automáticamente |
| D14 | Adoptada: resultados experimentales vacíos hasta medir | Separar protocolo, evidencia de auditoría y conclusiones |
| D15 | Propuesta: kernels primarios con instrucciones de 32 bits | Desactivar compresión/relajación automática; conservar pruebas C por separado y una política igual en todos los baselines |
| D16 | Adoptada: el core derivado se llama Kuntur; carpeta `kuntur/` | Renombrado a petición del autor; kirky-arqui identifica únicamente el origen y la evidencia histórica. XQDot4Z conserva su nombre como operación propuesta. |
| D17 | Verificada: referencia numérica v0.1 y pruebas M1 | `model/SPEC.md` y `docs/MODEL_STATE.json`; resultado exacto, sin acumulador, saturación ni truncamiento de entrada. No prueba RTL ni aceleración |
| D18 | Verificada: unidad aislada combinacional de cuatro vías, M2 | Restas S5, productos S13, parejas S14 y subtotal S15 con extensión explícita. Dos simuladores, lint y controles; `docs/RTL_STATE.json`. No fija ciclos, encoding ni recursos físicos |
| D19 | Verificada M3a: integración opcional, apagada por defecto | `ENABLE_XQDOT4Z`, metadatos D/E con flush/reset, operandos reenviados y resultado E→M→W. No introduce espera propia ni prueba timing físico |
| D20 | Conservada la base anterior a M3a | Archivo de entradas verificado por `docs/CORE_PRE_M3.json`; la regresión actual base y la integración tienen evidencias separadas |
| D21 | Verificada, M3b parcial: MUL opcional común, sin completar M/Zmmul | `ENABLE_MUL=0` por defecto, independiente de XQDot4Zi; low32 combinacional con forwarding y flush/reset. Mismo hardware para futuros B2/B3/D, sin timing ni speedup medidos |
| D22 | Verificada, M3b parcial: B3 aritmético aislado, `rtl/packed/xqdot4.v` | Cuatro vías U4×S8 sin restadores, mismo packing/h y anchos conservadores que D. P∈[−7680,7620]. Esta evidencia de unidad aislada no cubre encoding ni integración; se conserva sin reescribirla |
| D23 | Integración B3: XQDot4 en custom-1, 32 bits y h inmediato | Mantiene intacto custom-0 de XQDot4Zi. `ENABLE_XQDOT4=0` por defecto, dos GPR, forwarding y flush/reset comunes; ocho configuraciones verificadas. `docs/PACKED_INTEGRATION_STATE.json` |
| D24 | Adoptada antes de medir: B3 puede obtener Sa con XQDot4 y W=0x11111111 | Dos productos punto suman ocho activaciones; Sa se calcula una vez por vector/grupo y se reutiliza entre filas. Los fixtures ejecutan MUL/SUB de corrección en Kuntur. No se fuerza una suma escalar más costosa ni se atribuye aceleración a estos tests |
| D25 | Adoptada 2026-09-09: repositorio raíz en `main`, un commit inicial y futuros commits Karma en inglés | Kuntur se incluye como archivos normales. Historial previo preservado en `baseline/kuntur-history.bundle` y metadatos locales; sin submódulo, remoto ni publicación. `docs/VERSION_CONTROL.md` |
| D26 | Adoptada antes de medir: z como factor experimental, protocolo 0.2 y manifiesto inicial | ZC separado de ZS equilibrado, estratos explícitos, barrido N y reporte de todos los casos. Especialización estática sin extrapolar a ZR ni afirmar cota superior; despacho con inmediatos posible pero no implementado |
| D27 | Políticas pendientes bloquean kernels de rendimiento | Esqueleto común B3/D; desenrollado lógico, registros, planificación y reutilización de correcciones deben fijarse antes de escribirlos. Amenazas a la validez explícitas: el control de optimización no demuestra optimalidad |
| D28 | Seleccionada 2026-09-09 antes de medir: B1 contextual con descomposición acotada por máscaras | Una regla directa `(w-z)*a`, U4 para z=0, S4 para z=8 y S5 en los demás; expansión sin ramas por bit. Selección por simplicidad, no búsqueda de rendimiento ni optimalidad. Política, generador, código y pruebas se congelan antes de cualquier medida del kernel, incluido el piloto; cambios posteriores requieren revisión y repetición de pares afectados |
| D29 | Manifiesto 0.2 y protocolo 0.3: semillas de cobertura, rejilla intacta y piloto bloqueado | Diez semillas no son repeticiones estadísticas. Piloto prefijado de 32 casos de la rejilla: cuatro variantes, dos semillas, N=4, tres ZC y ZS fase 0; compara control, retiros, direcciones y stalls además de ciclos. No reduce cobertura por dos totales iguales; group_stride queda declarado inactivo para K=G |
| D30 | Seleccionadas 2026-09-09 antes de medir: cinco políticas comunes de codegen, manifiesto 0.3 y protocolo 0.4 | `shared_group_resident_skeleton_v1`: ocho elementos lógicos por iteración —una palabra de pesos y dos de activaciones—, grupo externo con activaciones residentes entre filas, reserva de registros sin spills internos idéntica en las cuatro variantes, izado de `z*Sa` solo cuando el calendario declarado repite z, y reducción de fuerza sobre constantes declaradas con planificación común. Consecuencia declarada: B3 corrige con una resta por fila en ZC y con multiplicación más resta en ZS, por el calendario y esta política, no por la arquitectura. El orden de recorrido no queda ejercitado con K=G. Se congelan como B1; seleccionarlas no desbloquea la campaña |
| D31 | Fijados y verificados 2026-09-09: eventos de medida y contadores, protocolo 0.5 y manifiesto 0.4 | `tb_measure.sv` abre en la aceptación de `BEGIN_PC` en decode y cierra en el último retiro de `END_PC`; `TEXT_END` delimita el kernel aparte porque en un bucle el PC de cierre no es la última instrucción del cuerpo. Se declaran el sesgo de llenado (`retired` frente a `retired_kernel`, acotado por la profundidad), la distinción entre ramas retiradas y redirecciones tomadas, y que las faltas ajenas al kernel no se le atribuyen. Identidad de rendición de cuentas: `cycles = retired_kernel + 3 + stall_load_use + 2*flush_taken_control`. Expectativas derivadas de la estructura del pipeline, no de una corrida; ocho programas dirigidos en dos simuladores, tres controles negativos y cuatro mutaciones del propio harness. `docs/MEASUREMENT_STATE.json`. No hay kernels de las variantes ni mediciones |
| D32 | Implementados y verificados 2026-09-09: kernels pareados D y B3 | K=G=32 y N∈{1,4,16}, dos semillas y cuatro asignaciones de z; 24 pares con salidas enteras idénticas entre variantes y contra el oráculo, en dos simuladores. Lista blanca de opcodes por variante: D no multiplica y ninguna variante entra en el espacio custom de la otra. El izado de `z*Sa` sigue el calendario declarado: una multiplicación en ZC y N en ZS. Las filas se emiten rectilíneas porque el zero-point es inmediato bajo especialización estática. B1 y B2 siguen pendientes, el bloqueador de kernels sigue abierto y no se ha calculado ningún speedup; los contadores observados se registran como tiempos de desarrollo. `docs/KERNEL_STATE.json` |
| D33 | Revisada 2026-09-09 antes de medir: política común v2, manifiesto 0.5 y protocolo 0.6 | `shared_tile_resident_skeleton_v1` era internamente contradictoria —ocho elementos por iteración implican bucle de K, y la residencia de activaciones a escala de grupo lo prohíbe al exigir registros distintos por iteración— y además inviable más allá de K=32: las palabras de activación son K/4, o sea 32 con K=128 y 128 con K=512, contra 31 registros. Habría fallado en RQ3 igual. v2 fija cuerpo de ocho elementos, K desenrollado, residencia a escala de cuerpo con ventana de dos palabras, y **deriva el recorrido de filas del ISA**: solo D está forzada a especializar bajo ZS, reportado como `required_row_bodies` (ZC 1/1/1/1; ZS B1=B2=B3=1, D=16). Toda especialización elegida va a `text_bytes`. Regla común: tomarla si y solo si no exige despacho. **Dirección declarada:** las dos correcciones se oponen en el ratio D/B3 —el bucle de filas lo sube, perder la residencia de grupo lo baja—; ninguna se eligió por su efecto. Revisada tras observar contadores de desarrollo, que se conservan; la justificación no depende de ellos. El comprobador gana invariantes cruzados entre campos congelados, que es lo que faltaba para que la contradicción no sobreviviera |
| D34 | Conservados como brazo diagnóstico 2026-09-09: kernels D y B3 de la política v1 | En vez de descartarlos se reetiquetan `inline_resident`: son el control que cuantifica la bonificación de desenrollado que el codegen de titular quita, y su verificación de 24 pares sigue válida. Se declaran sus tres defectos: z inmediato también en B3, direccionamiento absoluto que hornea la estructura rectilínea, y ninguna reducción de fuerza —con z=0 emite Sa, `mul` y restas que el protocolo le permite omitir, ~8% del kernel. `docs/KERNEL_STATE.json` |
| D35 | Implementados 2026-09-09 bajo la política v2: kernels D y B3 con gemelos estructurales | Residencia de cuerpo, K desenrollado, recorrido de filas derivado del ISA. La forma inline es el cuerpo de bucle replicado sin arista de retorno, de modo que el par difiere solo en el control y se puede restar; el contraste del par entre ZC y ZS separa la bonificación de desenrollado del costo de z. B3 toma ahora las reducciones que el protocolo le concede —con z=0 elide Sa y corrección, con potencia de dos usa shift— y bajo ZS lee z de un array en vez de materializarlo como inmediato, porque la regla declina el despacho. `required_row_bodies` se emite y se compara contra el piso del ISA. 24 pares con salidas idénticas en 92 brazos, dos simuladores. Sigue sin haber B1, B2, campaña ni speedup. `docs/KERNEL_STATE.json` |
| D36 | Implementadas 2026-09-09: las cuatro variantes bajo la política v2, manifiesto 0.6 | B1 y B2 comparten generador escalar y difieren solo en la multiplicación. El cuerpo lo definen los operandos, no las instrucciones, para que sea contable en variantes empacadas y escalares. 24 acuerdos a cuatro bandas con salidas idénticas sobre 188 brazos, dos simuladores. **JALR fuera de las cuatro listas blancas**: el salto indirecto es la firma del despacho que la regla declina, así que la regla se verifica en el binario. El cuerpo de fila de B1 excede el alcance de rama y su retorno se expande a salto directo, permitido solo en B1 y marcado por caso. Capacidad de instrucciones común fijada en 32 768 palabras por el brazo mayor, cerrando la compuerta de D10; el fetch es combinacional y no cuesta ciclos. Se descargan los bloqueadores de kernels y de capacidad/disposición; quedan cinco. Sin campaña, piloto ni speedup |
| D37 | Adoptada 2026-09-09 antes de medir: descomposición del hueco bajo ZS y cotización del control de bucle | Los brazos de titular no comparten forma de fila bajo ZS, así que el hueco crudo mezcla corrección y control. Comparar D contra el brazo de B3 de la misma forma lo parte en `estructural + aritmético` sin término estimado, y el runner comprueba que la suma cierra. El control de bucle se cotiza restando el gemelo: `3N−1` con arista simple y `4N−2` cuando el cuerpo de B1 supera el alcance de rama, ligado al campo que marca la expansión. La especialización forzada de D se reporta junto al hueco como palabras de código y `required_row_bodies`: son la misma decisión de interfaz por dos lados |
| D38 | Congeladas 2026-09-10 antes de cronometrar: ambas políticas, manifiesto 0.7 | `docs/POLICY_FREEZE.json` fija texto de política, generadores, pruebas y el ensamblado, desensamblado y texto de los 236 brazos, más la revisión de Git en la que cada archivo congelado está committeado. `check_project` lo re-deriva del árbol, así que un cambio posterior rompe `make check` apuntando al procedimiento; se verificó que salta editando un generador y una prueba. Descarga los dos bloqueadores de congelación; quedan dos |
| D39 | Materializado 2026-09-10 antes de medir: inventario completo de 2280 casos, manifiesto 0.8 | `benchmarks/inventory.json` fija tensores empacados, matriz de z, histograma efectivo, salidas esperadas y hash por caso, según `persist_before_measurement`. `check_project` lo recomputa desde las semillas en vez de confiar en el archivo. **Degeneración declarada:** en N=1 no hay z variable, así que los ajustes 0, 8 y 15 coinciden entre ZC y ZS y 120 casos comparten hash de entrada; se verifica que las colisiones son exactamente esas y solo en N=1, y que por tanto un contraste ZC/ZS en N=1 no es efecto de régimen. Descarga el bloqueador de tensores; queda uno |
| D40 | Registrado 2026-09-10: revisión previa a medir, manifiesto 0.9 y campaña `registered` | `docs/PREREGISTRATION.json` fija seis estados, el hash de las hipótesis del acta y las siete decisiones tomadas tras observar contadores, y recoge esos contadores releídos de la evidencia fijada para que no puedan reformularse. `check_project` lo comprueba, así que editar un estado registrado o una hipótesis rompe `make check`. Descarga el último bloqueador: la lista queda vacía. **Registrado no es ejecutado** — sin piloto, sin campaña y sin speedup calculado |
| D41 | Ejecutado 2026-09-10: piloto preregistrado de 32 casos; **no se toca el diseño registrado** | 16 pares de semillas a N=4, con programa, disposición, z, core, memoria y eventos fijos y comprobados por el runner. Las seis señales declaradas —ciclos, retiros, traza de PC/opcodes, resultados de branches, desglose de stalls y traza de direcciones— salieron iguales en los 16 pares, **mientras las salidas difieren en todos**: los tensores cambiaron y el tiempo y el control no. No demuestra independencia de todos los datos (una forma, un par de semillas, cuatro ajustes de diecinueve) y **no se reduce la rejilla**: eso exige la compuerta de reducción del manifiesto, que es una revisión deliberada. Registrado en `docs/PILOT_STATE.json`, fuera del preregistro, para no enmendar `campaign.json` ni el protocolo por un estado de ejecución |

El core tiene correcciones verificadas, registradas en
`kuntur/docs/CORE_CORRECTIONS.md` y fijadas en `docs/CORE_STATE.json`.
Se implementaron la referencia Python y la unidad aritmética RTL aislada.
La interfaz inmediata, su integración opcional y MUL escalar están verificadas.
La unidad B3 aislada conserva su propia evidencia de reconstrucción en el host.
Una batería posterior comprueba su integración y cuatro pares de fixtures
matriz–vector con suma, acumulación y corrección ejecutadas en Kuntur.
Los comparadores completos y los benchmarks de aceleración siguen pendientes.
La decisión D06 puede cambiar la viabilidad del régimen ZR: debe resolverse
antes de afirmar utilidad con metadatos dinámicos.
